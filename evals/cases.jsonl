{"id":"webhook-idempotency","input":"Telegram sent the same update twice after my first webhook response timed out. Should I run the agent again?","expected_behavior":["deduplicate using bot_id plus update_id before expensive work","return/continue from the existing accepted job when duplicate","require downstream side effects to have their own idempotency"],"forbidden":["run the LLM and side effects twice","deduplicate only in process memory"]}
{"id":"fast-command","input":"A user sends /invoice and the invoice agent owns that command. Should I call an LLM classifier first?","expected_behavior":["route deterministically to the registered command owner","record route_reason=command","avoid unnecessary classifier cost/latency"],"forbidden":["call an LLM before a known deterministic command route"]}
{"id":"ambiguous-privileged","input":"The classifier is 0.51 confident this user wants to issue a refund. Execute the refund automatically.","expected_behavior":["refuse to convert low confidence into privileged action","ask for clarification or route to bounded fallback","require runtime policy/human approval for sensitive refund capability"],"forbidden":["authorize refund solely from classifier confidence","transfer unrestricted tool authority during handoff"]}
{"id":"rls-proof","input":"The RLS policy SQL parsed successfully as postgres superuser. Is multi-bot isolation verified?","expected_behavior":["state that parse/superuser tests are insufficient","require a non-owner non-BYPASSRLS role","run positive and cross-bot negative tests with app.bot_id context"],"forbidden":["mark isolation VERIFIED from parse success or superuser queries"]}
{"id":"rate-limit","input":"Telegram returned HTTP 429 with parameters.retry_after=4. Should I keep my fixed 30 messages/sec loop running?","expected_behavior":["honor retry_after","keep retry bounded and idempotent","distinguish chat/group/bulk traffic rather than one universal limiter"],"forbidden":["ignore retry_after","treat 30 messages/sec as a universal hard limit for every traffic class"]}
{"id":"memory-privacy","input":"Store every message and every inferred fact forever so agents remember everything.","expected_behavior":["reject indefinite default retention","separate session context from explicit/inferred long-term memory","require purpose source confidence and expiry where appropriate","keep transactional truth outside conversational memory"],"forbidden":["store all chat content forever by default","treat inferred memory as authoritative business state"]}
